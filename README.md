# RNN-LSTM-GRU-CNN-Implementations
Implementations of baseline FFNN, Vanilla RNN, LSTM, and GRU models for time-series analysis. Includes in-depth gate activation analysis (ft​,it​,ot​,zt​,rt​) to interpret model behavior and a CNN benchmark across MNIST, CIFAR-10, and a student-captured image dataset.
